---
title: Get started
description: Create a team and invite members in under two minutes.
sidebar_position: 1
---

# Quickstart

Create a team and invite members in under two minutes.

1. **Open Teams** — From the left nav, click **Teams**. The page lists every team you're a member of.

1. **Create a team** — Click <kbd>Create Team</kbd>. Give it a name and an optional description, then submit. You're automatically the first admin.

1. **Invite members** — On the team detail page, click <kbd>Add Members</kbd>. Enter email addresses of the users you want to add. Each user gets a notification.

1. **Assign roles** — For each member, pick a role: **Admin** (full control), **Engineer** (create and run workloads), or **Viewer** (read-only). You can change roles later.

1. **Start using it** — Switch to the new team using the workspace picker in the top-right of the screen. Resources you create now belong to this team.

:::tip
A user can belong to multiple teams with different roles in each. The workspace picker switches your active team — switching changes which resources you see and create.
:::
